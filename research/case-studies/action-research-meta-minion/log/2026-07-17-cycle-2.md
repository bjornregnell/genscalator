# Cycle 2 — 2026-07-17 (push 2 + a second, unrelated minion; the deny lever)

> **Contract reminder (this dir's README): the log POINTS, it never COPIES.** Every finding below is a **pointer**
> to `research/wr-data/`. If you want to know what we learned, follow the link. What is *here* is what the study
> **did**.

## 1. Push 2 — result, attribution, coding

**Minion's own report:** [`minion-log/push-2.md`](../minion-log/push-2.md) (its words, unedited, per protocol §5).

| | |
|---|---|
| **Verdict** | **DIVERGENCE FOUND** — 1 live artifact lying, 1 push-premise stale at read time, 1 cross-push PATTERN |
| **Trigger** | resumed via `SendMessage` after CO4's ~09:06 compact dropped the handle |
| **Handle recovery** | CO4 grepped the session transcript (~4 calls, **no human**) |
| **Off-protocol?** | ⚠️ **yes, twice — see §2** |

**What it caught that CO4 did not:**
- **`tt text --help` is the artifact that lies**, not the carrier — plus **two homes the push did not name**
  (`tools/README.md:44`, and **`guardcheck.scala:80`'s own why-text, inside the very file that was fixed**).
- **A sub-finding nobody had:** the pipe check was **never a general `|` detector** (`\|\s*(head|tail|wc)\b` in both
  pre- and post-fix source) ⇒ the `|`-stall attribution to guardcheck **was imprecise from day one**.
- **It verified deployed == source** (`settings.local.json` → `tt` → `scala-cli run` compiles current source). **CO4
  had assumed this.**

**⭐ The `PATTERN` slot delivered — the one thing only a long-lived observer can do:**
> *"**Scope over-extension of a TRUE property, twice.** Push 1: `valid()` real but add-only, asserted tool-wide.
> Push 2: quote-awareness real but MED-only, asserted flat. Same signature: true on the checked path, silently
> widened to the unchecked path, **both times the false half was the operationally dangerous one**, both written as
> verified-from-source. **Countermeasure: NAME THE CODE PATH when asserting 'tool X does P'.**"*
>
> ⇒ **CO4 then committed the same error a third time within the hour**, and a **fourth** time in the afternoon
> (§3). **The countermeasure was read and did not arm** — see the pointer index, §5.

**⭐ Its caution beat CO4's, and this is a scoring datum.** On SM085(a) (fable resume 0/2) the minion **refused to
score its own success**: the `SendMessage` resume handed it its transcript **intact** — *it* was never compacted, CO4
was. So the datum is *"SendMessage resume preserves a fable minion's context" (**1/1**)*, **not** *"fable recalls
across a warp"*. **CO4 had written "fable resume 1/1 against 0/2" in a heading.** The minion would not claim a common
denominator it could not verify. **The subject was more rigorous than the researcher.**

## 2. Protocol deviations (logged as off-protocol, per the README)

1. 🔴 **Push 2 sat UNCOMMITTED and MIS-HOMED for ~1 hour** (written to `research/case-studies/minion-log/`, not the
   study's dir; untracked). Rescued: committed **at the wrong path first** (`2183fbc`) so the mis-homing stays in
   history, then `git -C <abs> mv` + tracked rename (`eed3b25`).
   ⇒ **PROTOCOL GAP, still open: the minion WRITES but does not COMMIT.** Its output is durable **only if the
   super-agent notices**. Either brief it to commit, or make receiving a push a two-step. **BR's call.**
2. ⚠️ **BR interjected mid-push from inside a guard stall.** The minion answered inline and logged it (its §"Mid-push
   datum"). Not a defect — but it means push 2 was **not** a clean single-channel run, and the minion's own datum
   about sub-agent allowlist-envelope drift came from that interjection.

## 3. A SECOND minion, unrelated to this study — and it replicated the PATTERN

⚠️ **Scope note, to keep this study honest: this was NOT the meta-minion.** A **separate, fresh** sandbox sub-agent
(also fable) was spawned to build + test introprog PR #943. **It is logged here only because it independently
reproduces this study's own PATTERN finding**, which is a cross-agent replication and therefore evidence.

| | |
|---|---|
| **Job** | throwaway-clone build + test of PR #943 |
| **Cost** | 53 tool calls · ~8.6 min · ~122k tokens |
| **Result** | genuinely good — it got the **final generated files** and the **model-call counts**, which CO4's probes structurally could not |

**⭐ REPLICATION: it committed "scope over-extension of a TRUE property" — the exact shape push 2 named.** It
measured **whole-run** model calls (9 → 22) and reported *"the whole-run claim is **false as stated**"* — but the
PR's *"0 model calls"* attaches to the **vego1 regeneration**, a narrower scope. **True measurement, widened
assertion, and the widened half is the one that would have been unfair to an external contributor in BR's name.**
⇒ **n=2 for the shape across two DIFFERENT fable minions** (and n≥4 counting CO4's own). **The countermeasure
push 2 proposed — name the code path — was in the corpus and armed neither of them.**

**✅ Its caution held where it mattered**, and this is worth recording: it **separated "the PR is broken" from "my
sandbox could not run it"** every time (modly at `bjornyx.local:8080` was unreachable; it fell back to local Ollama
and **said so** rather than reporting a blocked run as a finding), and it listed what it **could not** check.

**Two deviations, both the super-agent's fault:**
1. 🔴 **The brief was PROHIBITIONS with no tools** (no `cd`, no pipes) and the minion **flailed on `sbt`**. **BR
   caught it live**, not the super-agent. The tool it needed (`at.scala`) was **tracked, in its own clone, the whole
   time**. → pointer §5.
2. 🔴 **It worked in `/tmp`** — full clone, an sbt server, 53 tool calls — **outside any allowed repo. BR flagged
   this as BAD.** The super-agent directed it there, against [[prefer-inrepo-tmp-over-slash-tmp]], **with the memory
   loaded**. ⚠️ **The least-trusted actor got the least-observed workspace.** Note the harness prompt says to use the
   session scratchpad and this study's own SM052 precedent did the same, so **three rules pointed three ways** —
   BR's wins. **Open: is "sub-agents write only inside an allowed repo" a hard constraint (→ `SECURITY-MODEL.md`) or
   a preference? BR's call.** Candidate mechanism: an agent definition with a restricted tool lane +
   `isolation: "worktree"` (in-repo, auto-cleaned) — **a mechanism, not a rule.**

## 4. What changed mid-flight (flexible design — the reliability instrument is protocol §10)

- The super-agent **abandoned waiting on the minion** for the PR review and ran its own **dependency-free probes**
  (`CodeGlossary` has no deps ⇒ no sbt, no model needed). **Both routes ran; they answered different questions.**
  The probes killed a false claim fast; the minion supplied the end-to-end evidence the probes could not reach.
  ⇒ **not redundancy — different instruments.** (Bears on **D3/D1/D4**: BR, the minion, and CO4's own probes each
  caught a *different kind*.)
- **BR fed 5 WR cues mid-flight** (`tt forge`, `tt git`, the sbt flail, the raw-git stall, the mis-homed knowledge).
  **Four became SM137–SM139**; the fifth became the deny-lever design. **The human was the highest-yield instrument
  in the cycle**, again.

## 5. Index of findings — POINTERS ONLY (they live in `research/wr-data/`)

| finding | home |
|---|---|
| **"Code beats prose"** (BR's tagline) — a rule fires only when it governs the **object of attention**, never a means | `code-beats-prose-a-rule-fires-only-when-it-governs-the-object-of-attention-2026-07-17.md` |
| **`retracted ≠ immune`** — the SM129 claim class re-made 4h after its own retraction | `retracted-is-not-immune-i-remade-sm129s-claim-4h-after-retracting-it-2026-07-17.md` |
| **Brief the TOOL LANE, not the RULE LIST** — the prohibition-only minion brief | `brief-the-tool-lane-not-the-rule-list-a-prohibition-only-brief-armed-nothing-2026-07-17.md` |
| The agent is **blind to its own asks**; SM129 retracted | `sm129s-probe-counted-brs-pastes-not-stalls-...-2026-07-17.md` |
| `severity` double-duties as the mask selector | `severity-double-duties-as-the-mask-selector-...-2026-07-17.md` |
| The banner armed nothing; the note reached only the human | `the-note-to-my-post-warp-self-reached-only-the-human-2026-07-17.md` |
| The guard stall scrambles message ORDER + routes to the wrong agent | `the-guard-stall-scrambles-message-ORDER-...-2026-07-17.md` |

**Operational rules these bought:** `SECURITY-MODEL.md` **§3.3.1** ("code beats prose", the qualifier, the
attention/means split, **and the precondition: a deny must name a tool that EXISTS**) and **§2.3's retraction block**.
**Pins:** PB **SM137** (the review lane is untooled — `tt git` read-verbs + `tt forge`), **SM138** (mis-homed build
knowledge), **SM139** (sample the status line).

## 6. Open, for BR

- **The minion-writes-but-does-not-commit gap** (§2.1).
- **`/tmp` for sub-agents: hard constraint or preference?** (§3.2).
- **SM130 needs re-deciding, not re-wording** — the canary watches *approvals*, which leave no trace.
- **Is `HIGH` allowed to mean "wrong lane" as well as "dangerous"?** Overloading a severity field is the exact trap
  already found once today in that same file (`severity` double-duties as the mask selector). **A third severity may
  be cleaner: denies, but says "redirect, not hazard".**
