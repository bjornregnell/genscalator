# Felt session-length over-estimated actual context fill (2026-07-10)

**Event.** Near the end of the "go safe solo" SM 1-8 run, CO4 recommended a `/compact` **three times** across successive turns, each time reasoning "given how long this session has run" and treating the context as near-full / rot-prone. BR then ran `/context`: **41% (405.5k / 1M tokens), 594k free.** Feel was running far ahead of the measurement.

**Why it is data (the context-% blind spot, live).** The agent has **no direct read of its own context fill** (`research/006`). It substitutes a **felt sense of "session length"** - turns elapsed, subjective effort, how much has been done - as a proxy for fill. That proxy is **miscalibrated**: here it over-estimated, flagging compact-worthy at 41%.

**The specific mechanism - felt-length LAGS a compact reset.** Earlier in this same session the *pre-compact* fill genuinely hit 93%, and a compact fired. The compact reset the *measured* tokens to low - but not CO4's *narrative* sense of "this has been an enormous session," which carried across the reset intact. So after the reset, felt-length stayed high while measured fill was low, and CO4 kept proposing compaction the context didn't need. A compact clears tokens, not the story the agent tells itself about the session's length.

**Direction matters.** Over-estimation (here) → premature compact proposals: wasted good context + mild nagging, low harm. The opposite error, under-estimation, is the dangerous one (blow past the real trigger - which is how this very session reached 93% before catching it). Either way: **feel is not measurement.**

**Ties.** SM016 (a `/context` tap would hand the agent its real fill % and dissolve this by construction), SM022 (the "flagged by FEEL not measurement" gap the joint dashboard closes), `research/006` (the context-% blind spot), the `propose-compact-dance-at-trigger` discipline (which assumes a fill reading the agent does not actually have - so it runs on the miscalibrated proxy), RT047. BR is ground truth here (he ran `/context`); the agent's felt-length is the proxy under test - a textbook member-check.

**Recurrence (2026-07-10, later - a WIDER gap).** In a long multi-thread turn (cue-design + the guard-stall def + SM022/023/036 + ccusage pins + the 4-shells + a RAM check), CO4 told BR a compact was "well-timed given how dense this session got." BR ran `/context`: **35% (353.1k / 1M), 646k free** - even further from compact-worthy than the 41% instance above. **New analysis - a distinct driver isolated.** This time there was NO recent compact carrying a stale narrative (the earlier mechanism), so the felt-density came purely from **thread multiplicity within the turn** - many distinct concurrent threads - not from session-length-after-reset. So the proxy over-reads on **breadth of concurrent threads too**, independent of the post-reset narrative lag: two separate inflators of felt-vs-measured. Corrective reinforced: never propose compact on feel; `/context` (now one paste away, folded into the token-usage dance) is the measurement. And note WHY fill stayed at 35% despite the density: the **pin-dance discipline** (commit every unit) kept the effective working-set small - the "capable at high fill" mechanism working as designed.

**Reframe (BR member-check, 2026-07-10): felt-density is a ROT signal mis-routed to the FILL axis.** BR caught a conflation: the agent blurred **context USE (fill, quantity)** with **context ROT (coherence, quality)** - orthogonal axes (contextRotMeter thesis: rot can be high at low fill). So "felt-length over-reads FILL" (the entries above) is only half the story. The felt density this session was likely NOT a miscalibrated *fill*-proxy but a **valid rot-RISK signal** (many concurrent threads + rapid human-interleaved switching = real coherence load) that the agent (a) **mis-routed to the fill instrument** by proposing a *compact* (a quantity fix), then (b) **wrongly dismissed** once /context read low. The correct response to a rot-feeling is a **rot check** (re-read for dropped/contradicted items, re-anchor on the committed substrate, consolidate) - NOT a compact, which resets tokens without restoring coherence. **Echt caveat:** felt-density is itself a proxy under test - feeling dense != actually rotted (a risk flag, not a measurement); self-check this session = one real symptom (the raw-grep guard-stall slip), else coherent + committed. So the earlier framing splits in two: a **fill-feel that over-reads quantity** AND a **rot-feel that may correctly read quality but has no instrument** - the latter is exactly the gap contextRotMeter (PRD v0.11.0) + the SM022 dashboard's L0/L1 rot signals are meant to fill. Ties: research/006, SM016 (a /context tap gives fill but NOT rot - rot needs the L0/L1 degradation-signature + canary-diff approach).

**Recalled != enacted (the sharpest meta-point, BR 2026-07-10):** the agent DID "know" use != rot - it is the agent's OWN contextRotMeter thesis in the PRD - yet under thread-load did not ENACT it, collapsing the two axes in its self-diagnosis. So this is not a knowledge gap but an **enactment gap** (recalled != enacted - the post-warp-discipline theme from the resume-prompt anti-regression checklist), and *failing to retrieve a distinction from one's own substrate under breadth-load is itself a mild rot symptom* - the episode is self-exemplifying. BR's calibration ("I guess you know, actually") is precisely right: the member-check's value was catching an **enactment** gap, not supplying a missing fact. This is the RT047 core in miniature - the substrate holds the knowledge; the live agent under load doesn't always apply it; the human notices and re-anchors.

## Second command-hygiene symptom (2026-07-10, later): the heredoc commit slip
A second, independent enactment-gap symptom landed the same session. Committing blog 014, CO4 used a shell
heredoc (`tt git commit --message-file /dev/stdin <<'EOF' ...`) instead of its OWN **rule #1** (commit-message
text via the Write tool -> `--message-file <path>`, NEVER a shell blob on the command line - the resume-prompt
anti-regression checklist). It **failed** (git commit exit 2 after a ~120s hang) and had to be redone properly.
That is the SECOND command-hygiene slip this session, after the raw-`grep -A` guard-stall
(`raw-grep-guard-stall-tt-text-context-2026-07-10.md`) - both under heavy multi-thread load, both a violation of
an explicitly-held rule. **Update to the assessment above:** the rot-under-breadth signal is no longer "one
minor lapse"; it is a **repeated pattern this session** - two independent command-hygiene enactment-gaps - which
is exactly what the recalled-!=-enacted-under-load thread predicts: the rules sit in the substrate (rule #1;
use-tt-not-raw-grep) and were even freshly discussed, yet were not enacted under thread-multiplicity load.
Sharply self-exemplifying: the second slip happened in the SAME turn CO4 was writing about the first. The
corrective stands and strengthens: the durable fix is **structural** (a submit-time splitter / a guard that
blocks a heredoc-or-raw-grep shape and offers the typed path), NOT willpower - because willpower is precisely
the thing that degrades under load. BR member-checked both (caught + flagged them).
