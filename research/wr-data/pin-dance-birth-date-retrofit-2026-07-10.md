# The pin-dance's own birth date, retrofitted to the minute (2026-07-10, SM044a-S5)

The evidence-timestamp enhancement's most fitting first target is the **pin dance itself**: the notes said only
"BR 2026-07-04" for the note/pin split and the etch→pin rename, with no time-of-day. Retrofitted via `tt wr stamp`
(the rare token `etch`, `--human`) on the muntabot project dir, cross-checked against genscalator git commit dates.

## The timeline (evening of 2026-07-04; UTC → CEST = +02:00)
- **22:03:31 CEST** (`20:03:31.984Z`, session `240e00c3`) — BR runs a **typing-ergonomics test** ("etch etch etch
  etch …"), probing whether the candidate cue word is comfortable to type. The cue is being *chosen*, embodied.
- **22:12:29 CEST** (genscalator commit `34e5c7f` "etch/note split: propagate the rename …") — the **note / etch
  split** lands: `note` = notice-in-passing, `etch` = durable-save.
- **22:16:50 CEST** (`20:16:50.703Z`) — BR: *"lock it, etch the go dance as it has several steps …"* — the `etch`
  durable-save cue in **active use**, applied to its first real target (the go dance). (Agent acts on it 3 min later:
  commit `7898dbf` "foundations + 005: etch the go dance", 22:19:27 CEST.)
- **23:22:57 CEST** (genscalator commit `1a5092c` "rename the durable-save cue/dance etch to pin (resolves the
  echt/etch near-anagram collision)") — the **etch → pin rename**. This is why the memory is `cue-note-vs-pin.md`
  and an older `cue-note-vs-etch.md` file-history exists.
- **23:27:53 CEST** (`21:27:53.857Z`) — BR: *"that is my etched no **pinned** stuff …"* — the human **enacting the
  rename in real time**, catching the old word mid-sentence. A charming human-in-the-loop datapoint: the rename was
  minutes old and the reflex still reached for "etch".

## Reading
The pin dance was born across ~80 minutes on the evening of 2026-07-04: cue chosen (~22:03), note/etch split
committed (~22:12), etch put to work (~22:17), renamed to **pin** (~23:23), the human live-correcting himself to the
new word (~23:28). "Pin" — the word the whole substrate now runs on — is **~1h20m younger** than "etch" that evening.

## Disposition (BR-gated dance text)
This is the S5 *lookup* result, recorded here durably. The natural home for the birth-date itself is the **pin-dance
entry in `docs/foundations.md`** and the memory **`cue-note-vs-pin.md`** — but those are dance-definition edits
(proposals P1/P3), **BR's call**, so they are NOT auto-applied. Flagged for BR to fold in a one-clause "(born ~22:12
CEST 2026-07-04, renamed etch→pin ~23:23)" if he wants the symbolic touch. Ties:
[[wr-stamp-dogfood-transcript-format-2026-07-10]], [[cue-note-vs-pin]], [[raw-data-append-only]].
