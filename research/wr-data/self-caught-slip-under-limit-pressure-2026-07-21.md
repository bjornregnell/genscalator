# Self-caught mechanical slip under limit pressure + the introspection datum

2026-07-21 16:21:48 clock-read. BR cued "WR data on that echt vigilance about rot suspision
and your introspection on that", the introspection itself cued by his Fable-gränsransonering
question (what happens when CF5 stalls at the 5h cap: auto-degrade to CO4?).

## The specimen (mechanical, verifiable)
At 16:14-16:16, racing a 98%-spent session limit, the agent committed the SM189 pin to the
work repo but passed the WRONG --message-file: the stale SM187 message was reused, so commit
`666b3bd` carries a mislabel ("SM187 pinned api-css bounded look, ColdStart timer rider")
over SM189 content. Pushed x3 before noticed. Remedy per [[keep-the-ball-game-retract-by-
annotating]]: confessed in-feed within the same turn, unprompted; the next PB commit notes it;
history not rewritten.

## Why it is a good datum
1. **Timing**: the slip landed EXACTLY in the window where pressure predicts it (limit racing,
   compressed replies, batched mechanics) — consistent with the mechanical-precision-degrades-
   first family (meta-minion brief failure mode 3; the CLOBBER-REPAIR and stamp-correction
   specimens). RotVigil was ACTIVE, and vigilance did not prevent it — but it did catch it
   one turn later, unprompted. Vigilance as detector, not preventer: same verdict as the
   ember-warming probe ("a rate reducer, not a switch") now replicated on the SUPER-agent.
2. **Symmetry with the minion ledger**: same day, the minion's raw `ls` (1/10) was caught by
   the super-agent; the super-agent's wrong message-file (1 in ~6 commits this hour) was
   caught only by itself, after the fact. Nobody in the loop self-flags in the act; every
   layer needs an outside eye or an after-pass. The meta-minion exists for exactly this.
3. **The introspection half (BR's Fable-gränsransonering question)**: asked what happens at
   the cap (his guess: harness auto-degrades CF5 -> CO4), the agent's honest answer was that
   it CANNOT observe its own substrate swap from inside, and pointed at the statusline model
   chip as the objective instrument instead. That is push-15's cross-push pattern
   ("world-claims solid, self-claims the soft target") applied prospectively by the agent to
   itself — the introspective report declining to be its own evidence and delegating to an
   external gauge. Whether that stance survives an ACTUAL degrade (would a downgraded model
   still know to distrust itself?) is an open, testable question for the next cap window.

## Open follow-up
- If/when a cap-degrade is observed: compare slip rate and self-catch latency across the
  chip flip. Prediction registered now: mechanical slips rise before prose quality drops.
