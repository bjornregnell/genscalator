# SM136 content-confirm: 047 homing is NULL-mis-homed by content read (2026-07-17)

**The confirm step the first-pass triage flagged as open** (`sm136-wr-data-vs-study-log-homing-triage-2026-07-17.md`
§"Method limit": *"a full confirm = read the notes and check none is secretly a 047 observation"*). Done here:
every 07-06/07 `wr-data/` note read to its finding-statement and adjudicated. **The triage's title-level LOW
verdict holds at the content level — and tightens to NULL.**

## The criterion (sharpened during the read)

The useful question is **not** *"is this note 047-relevant?"* — several are. It is: **is this a CURATED study
observation that belongs in 047's `## Study log`, mis-filed to `wr-data/` by the "wr data" reflex — vs a RAW
specimen, which CORRECTLY lives in `wr-data/`** (append-only, [[raw-data-append-only]]) with a 047 cross-ref?
047's own architecture sets this: its 16 curated observations live **inline** as `## Study log` sections; the
raw specimens live in `wr-data/`. So a 047-relevant raw specimen in `wr-data/` is **correctly homed, not
mis-homed.**

## Verdict per note (14 notes; see below on why 14)

**NULL curated-observation mis-homing.** Every note is one of:
- **General WR finding** (off 047's warp/restart topic entirely): `broad-allowlist-aversion` (allowlist +
  ties 024-affective), `terse-precise-comms` (comms/002), `collaboration-improves-br-english` (register/pubs),
  `tt-git-pull-gap-discovery` (tooling), `interpreter-allowlist-hazard` (allowlist security),
  `delegation-dance-and-dilemma` + `subagent-harness-control` (delegation/RT049), `platform-update-digest`
  (toolchain state, explicitly cross-reffed to 047 as a version confound, not an observation),
  `SYNTHESIS-structure-over-willpower` (a synthesis that POINTS at raw specimens, correctly).
- **Raw specimen that feeds 047 but is correctly in `wr-data/`**: `command-hygiene-regression` (its §4/S4 is
  literally *"the sharpest warp-fidelity datapoint"* — first tool call post-`/compact`), `instrument-deadlock-
  and-throughput` (post-warp latency data), `compound-command-approval-halt` (an enactment-leak specimen),
  `guard-stall-invisible-to-agent`, `shell-blob-fallback-regression`. **All raw → correctly homed.**

## Refinement 1 — the candidate set was 14, not 13 (triage under-counted)

The triage's list omitted **`guard-stall-invisible-to-agent-2026-07-07.md`** — a 07-07 note ("5th
command-hygiene specimen", from the Go #2 ralph loop). It is **not** mis-homed (a general guard-stall-
invisibility + time-blindness finding), so the verdict is unchanged — but **the enumeration was incomplete**,
and an incomplete candidate set is exactly how a real mis-homing could have slipped a title-level pass. Logged
as a method note: a filename/date triage needs its own completeness check.

## Refinement 2 — the read VALIDATES 047's two-home data architecture

Several notes ARE 047-relevant enactment/warp evidence yet correctly sit in `wr-data/`. That is not a defect —
it confirms 047 kept **raw in `wr-data/`, curated inline**, exactly as [[raw-data-append-only]] + [[study-log-
definition]] prescribe. ⭐ **Positive side-finding:** 047's raw evidence base **spans both homes by design** —
at least 5 `wr-data/` specimens are 047-grade enactment/warp data (`command-hygiene-regression` §1/§4,
`compound-command-approval-halt`, `guard-stall-invisible-to-agent`, `shell-blob-fallback-regression`,
`instrument-deadlock-and-throughput`). **If 047 is ever written up, this is its raw corpus** — richer than the
inline `## Study log` alone suggests. (A cross-ref-completeness note, not a rehoming action.)

## SM136 status + the one thing still for BR

- **SM136 (mis-homing sweep): RESOLVED.** Habit-mis-homing ruled **LOW→NULL** by content read. No rehoming
  action needed; 047's data is where it belongs.
- **Unchanged and still BR's call (recommend, don't decide):** stand up a **successor study 048** for the
  post-047 warp cluster (~15 `wr-data/` notes, 07-10 onward: `carried ≠ armed` / `carried ≠ still true`,
  reflex-continuity across a warp, the note-to-post-warp-self failure), or keep it as general corpus. The
  cluster is a **ready evidence base** either way. This content-confirm does not bear on that decision — it
  only closes the mis-homing question.

## Ties
`sm136-wr-data-vs-study-log-homing-triage-2026-07-17.md` (the first pass this confirms) · [[study-log-definition]]
· [[raw-data-append-only]] · study `047-fresh-restart-fidelity` · the post-047 warp cluster (candidate 048).
