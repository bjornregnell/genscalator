# SM136 triage: is any `wr-data/` note really a study-log observation mis-homed by habit?

**2026-07-17, agent AFK-solo pass.** SM136 (BR): *"we might later solo do a sweep to see if some wr data actually
should belong [in a study's log] if i just cued 'wr data' and none of us realized that."* This is the **bounded
first pass** — a filename/date/title triage, **not** a full-text adjudication (that confirm step is flagged below).

## Scoping finding (narrows SM136 a lot)

- **The study universe is ONE study:** `research/047-fresh-restart-fidelity.md` is the only file carrying
  `## Study log` sections (16 of them). ⇒ SM136's sweep reduces to a single question: *"which `wr-data/` notes are
  really observations from study 047?"* (cf. [[study-log-definition]]).
- **047's domain:** does the externalized substrate carry a session across a **warp / fresh restart** (full-context
  vs fresh-reconstructed). **Active data-collection window = 2026-07-06 → 07-07; closed at n=2 warps** (HD in-file).
- **047 homed its own data correctly — INLINE.** Its 16 observations live as `## Study log` sections *inside* 047,
  which is exactly right. So the "we filed a study observation to `wr-data/` by reflex" failure, **for 047, did not
  obviously happen** — its records are where they belong.

## The triage (by filename + date + title — see method limit)

**Candidates that COULD be mis-filed 047 data = `wr-data/` notes in 047's active window (07-06/07) AND on 047's
warp/restart topic.** The 07-06/07 `wr-data/` notes are:
`broad-allowlist-aversion` · `platform-update-digest` · `instrument-deadlock-and-throughput` · `terse-precise-comms`
· `command-hygiene-regression` · `SYNTHESIS-structure-over-willpower` · `collaboration-improves-br-english…` ·
`compound-command-approval-halt` · `tt-git-pull-gap-discovery` · `interpreter-allowlist-hazard` ·
`delegation-dance-and-dilemma` · `subagent-harness-control` · `shell-blob-fallback-regression`.
⇒ **By title, NONE are on 047's fresh-restart topic** — they are allowlist / command-hygiene / delegation / comms
findings, i.e. correctly-general WR-programme notes. **Preliminary verdict: LOW mis-homing for 047.**

## The reframe worth more than the null (the real finding)

There IS a large, coherent `wr-data/` cluster on 047's *topic* — but almost all of it is **dated AFTER 047 closed**
(07-10 onward), so it is **not** 047 data (the study was done). It reads instead like the seed of a **successor
study**. The cluster (warp / clear / cold-start / post-compact / reconstruction fidelity):
`does-harness-disinformation-survive-a-compact` (07-10) · `bare-auto-compact-stale-substrate` (07-11) ·
`resume-reloads-context-no-rot-reset-fresh-session-is-the-warp`, `warp-trades-rot-for-memory-loss…`,
`br-observes-co4-personality-changed-after-memory-loss`, `agent-has-no-phenomenology-of-absence`,
`active-skill-still-cold-starts-dormant-reflexes-regress`, `compaction-regresses-fine-grained-reflexes` (07-13) ·
`preparing-a-warped-agent-clear-restart-is-rate-distortion`, `cold-start-gap-closed…`,
`waking-up-after-a-usage-limit-head-hit` (07-14) · `two-error-classes-rot-vs-structural-and-what-a-clear-buys`,
`post-compact-is-highest-risk-window-for-mechanical-bash-regression` (07-16) ·
`the-note-to-my-post-warp-self-reached-only-the-human`, `statusline-sample-fresh-clear-plus-resume-prompt-is-4pct-fill`
(07-17). **~15 notes.**

⭐ **So SM136's habit-mis-homing risk is LOW, but the sweep surfaced something better: a study-sized body of
post-047 warp evidence with no study to belong to.** The actionable question flips from *"rehome mislocated notes"*
to *"does the post-047 warp cluster warrant a successor study (048?) — `carried ≠ armed` / `carried ≠ still true`,
reflex-continuity across a warp, the note-to-post-warp-self failure?"*

## Method limit + honest recommendation

- ⚠️ **This is filename/date/title-level, not a content read.** A full confirm = read the 13 07-06/07 notes above
  and check none is secretly a 047 observation. Cheap (13 short notes), but a **judgment call best made with fresh
  context or jointly** — not extended here under a long AFK session (echt over volume).
- **Recommendation:** (1) treat 047's homing as **provisionally clean** (confirm with the 13-note read); (2) take
  the real decision to BR — **stand up a successor study** for the post-047 warp cluster, or keep it as general
  corpus. Either way the ~15-note list above is the ready-made evidence base.

## Ties
[[study-log-definition]] · [[raw-data-append-only]] · study `047-fresh-restart-fidelity` · SM136 (this pass) ·
the warp/restart cluster (candidate study 048).
